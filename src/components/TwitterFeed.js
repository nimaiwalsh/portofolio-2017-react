import React, { Component } from 'react';

class TwitterFeed extends Component {
  componentDidMount() {
  }

  render() {
    return(
      <div>No timeline</div>
    )
  }
}

export default TwitterFeed